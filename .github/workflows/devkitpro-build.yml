name: Libretro Devkitpro disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled for Android-only fork
        run: echo "This workflow is disabled because this fork only builds Android libretro artifacts."
